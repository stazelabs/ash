# ash local env — copy to .env or .env.local and fill in. Both are gitignored;
# .env.local takes precedence if both exist. Used by `make validate`.

# Anthropic API key for `encexplore validate` (cl100k-vs-Claude cross-check).
# Get one at https://console.anthropic.com/. The count_tokens endpoint is
# inexpensive but not free — see CLAUDE.md §Token cross-validation.
ANTHROPIC_API_KEY=
